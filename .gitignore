dist/
_site/
_cache/
node_modules/
.DS_Store
.vercel
*.log

# Windows metadata files
*:Zone.Identifier

# Resume source — managed locally via `deno task resume`, not tracked in repo
resume.json
main.typ
