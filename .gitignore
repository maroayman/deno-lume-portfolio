# Files & Misc.
dist/
_site/
_cache/
node_modules/
.DS_Store
.vercel
*.log

# Windows metadata files
*:Zone.Identifier

# Resume source — managed locally via `deno task resume`, not tracked in repo
resume.json
main.typ

# Directories
.directory

# Cover/content images — pulled digest-pinned from GHCR at build time
# (CI, Vercel, `deno task images:pull`), never committed
src/public/images/
